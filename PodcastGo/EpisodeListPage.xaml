<Page
    x:Class="PodcastGo.EpisodeListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PodcastGo"
    xmlns:models="using:PodcastGo.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MasterColumn.Width" Value="*" />
                        <Setter Target="DetailColumn.Width" Value="0" />
                        <Setter Target="DetailGrid.Visibility" Value="Collapsed" />

                        <Setter Target="DetailColumn.MinWidth" Value="0" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MasterColumn.Width" Value="400" />
                        <Setter Target="DetailColumn.Width" Value="*" />
                        <Setter Target="DetailGrid.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MasterColumn" Width="400"/>
            <ColumnDefinition x:Name="DetailColumn" Width="*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Pane: Episodes -->
        <Border x:Name="MasterPane" Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0">
            <ListView x:Name="EpisodeListView" ItemClick="EpisodeListView_ItemClick" IsItemClickEnabled="True">
                <ListView.Header>
                    <StackPanel>
                        <TextBlock x:Name="PodcastTitleTextBlock" FontSize="24" FontWeight="Bold" Margin="16" TextWrapping="Wrap"/>
                        <StackPanel Margin="16,0,16,12">
                            <TextBox x:Name="SearchBox" PlaceholderText="Search notes and episodes" Margin="0,0,0,8" TextChanged="SearchBox_TextChanged"/>
                            <CheckBox x:Name="ShowAllCheckBox" Content="Show all episodes" Checked="ShowAll_Changed" Unchecked="ShowAll_Changed"/>
                        </StackPanel>
                    </StackPanel>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Episode">
                        <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Notes}" FontSize="12" Foreground="Gray" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="Listened" Visibility="{Binding IsListenedVisibility}" Foreground="Green" FontSize="12"/>
                            </StackPanel>
                            <SymbolIcon Grid.Column="1" Symbol="Download" Visibility="{Binding IsDownloadedVisibility}" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Right Pane: Details -->
        <Grid x:Name="DetailGrid" Grid.Column="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <TextBlock x:Name="DetailTitleTextBlock" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Text="Select an episode" TextWrapping="Wrap" />
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,24">
                <Button x:Name="MarkListenedButton" Content="Mark as listened" Click="MarkListened_Click" Margin="0,0,12,0"/>
                <Button Content="Mark All Previous Listened" Click="MarkPreviousListened_Click"/>
            </StackPanel>

            <Grid Grid.Row="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Notes" FontSize="20" Margin="0,0,0,8"/>
                <TextBox x:Name="NotesTextBox" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" TextChanged="NotesTextBox_TextChanged"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
